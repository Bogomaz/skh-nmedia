NMedia - это социальная сеть, которая должна быть реализована в рамках прохождения курса Android-разработчик с нуля. 

19.12.2025
1. Сделала ещё одну ветку, в которой реализовала передачу аргументов через safe args.
2. Ветку bundle слила в master
3. В итоге, в проекте сейчас ветки:
    - master (main) - главная
    - mvvm (слита в главную 04.12.2025)
    - bundle (слита в главную 19.12.2025)
    - safe_args

=====================
18.12.2025 - 19.12.2025
1. Реализовала функцию "читать пост полностью". 
По клику приложение показывает фрагмент с полным текстом поста. В этом фрагменте работают:
лайк, репост, клик по видео, функции меню. 
2. При удалении пост удаляется и пользователь возвращается на предыдущий фрагмент (лента)
3. При редактировании пользователь переходит на фрагмент для редактирования / создания поста.
4. Передача данных между постами происходит через Navigation Component и bundle с использованием делегатов. 

=====================
14.12.2025 - второй подход
1. Выполнила примеры из лекции: перенесла все интерфейсы из activity на fragment. 

=====================
14.12.2025 - первый подход
1. Выполнила примеры из лекции, иллюстрирующие работу с shared preferences
2. Реализовала работу с файлами. Теперь тестовые данные помещаются в файл при инициализации и берутся оттуда.   
При добавлении/изменении/удалении постов, эти изменения сохраняются в файле.
3. Исправила объект Post. В нём больше нет списка постов. Теперь он работает только с отдельными постами и меняет их содержимое.
4. Поправила баг с дублирующимися айдишниками постов.

=====================
11.12.2025
1. Добавила второе activity для редактирования/создания поста
2. Реализовала переходы между activity с помощью contract.
3. Добавила поля для ссылки на видео в объект Post (эти поля хардкодно заполняются при инициализации тестовых данных, добавить ссылку на видео из интерфейса нельзя).
4. Поправила мелкие опечатки в названиях файлов. 

=====================
04.12.2025
1. Применила компоненты и стили Material
2. Сгенерировала цветовую тему, отличную от умолчательной, и применила её в проекте
3. Поправила мелочи по форматированию чисел (кол-во лайков, репостов и пр.)

=====================
01.12.2025
1. Добавила CRUD-функции, как показывали на уроке, но с учётом особенностей своего проекта.
2. Сделала функцию cancelEdit в PostViewModel
3. Изменила viewModel.edited.observe в MainActivity так, чтобы видимость элементов интерфейса зависела от состояния даннных.
4. Убрала из обработчика кнопки save код, управляющий UI

=====================
28.11.2025
1. Упростила модель: удалила лишние классы, эксперименты с наследованием. Упростила структуру класса данных Post
2. Переписала методы в PostService с учётом новой структуры.
3. В PostRepository переименовала get и like в getAll и likeById соответственно. 
4. В PostRepositoryInMemoy 
   4.1. Изменила тестовые данные, создала список постов.
   4.2. Изменила LiveData, чтобы обеспечить работу со списком постов
   4.3. Сделала реализацию для методов likeById и getAll с учётом всех изменений в проекте. 
5. Изменила PostViewModel для работы с новыми методами репозитория.
6. Добавила RecyclerView и адаптер для вывода постов
7. Функцию для выбора нужной иконки лайка перенесла в PostViewHolder
8. Функцию для форматирования UnixTime вынесла в отдельный класс

=====================
26.11.2025
1. Создан интерфейс PostRepository  
2. В классе PostRepositoryInMemoy реализованы методы like и repost этого интерфейса.
Эти методы не умеют изменять post напрямую, но они умеют обращаться к методам класса PostService, который отвечает за манипуляции с постами.  
2.1. like обращается к PostService.likeHandler, а тот, в свою очередь, ставит / снимает лайк данного пользователя 
2.2. repost обращается к PostService.repostHandler, а он увеличивает счётчик репостов.  
3. Добавлен класс PostViewModel, а в нём обёртки для функций репозитория.
4. Main Activity изменён таким образом, чтобы при возникновении событий вызывались методы из viewmodel. 
Логика работы с постом и классом PostService удалена из Main Activity.
5. Вспомогательные функции для форматирования времени и выбора иконки лайка пока остались в Main Activity


=====================
24.11.2025
1. Перенесены классы данных из курса kotlin: 
Post, Record, Comment, Attachment, User и другие, помельче.

2. Перенесён класс для работы с постами PostService.
В этот класс добавлены методы:
2.1. likeHandler, repostHandler. Они отвечают за изменения полей likes и reposts в объекте post.
2.2. convertNumberIntoText и formatNumberWithSuffix. Они отвечают за конвертирование числа в строку и форматирование этой строки. 

3. В FeedFragment.kt добавлены методы 
3.1. formatUnixTime - конвертирует дату из Int unixtime и форматирует её
3.2. selectImageResource - выбирает правильную иконку для like 


=====================
08.11.2025
1. Настроен деполй
2. Выполнен первый коммит.

